/// @description Insert description here
// You can write your code in this editor
if (owner.isAttacking == 0 && owner.fx = id)
{
	instance_destroy();
}
if (owner.fx2 = id && dontswitch == false)
image_xscale = -owner.facing;
else if (dontswitch == false)
	image_xscale = owner.facing;

if (follow == true)
{
	x = owner.x + xOffset;
	y = owner.y + yOffset;
}







