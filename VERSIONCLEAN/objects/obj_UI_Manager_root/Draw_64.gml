/// @description Debug
/*if ( DEBUG == true)
{
	draw_set_font(-1);
	var dis_y = display_get_gui_height();
	var dis_x = display_get_gui_width();
	var border = 40;

	var dist = -30;
	draw_set_color(c_white);
	for (var i = 0; i < array_length(Stack); ++i) {
			draw_text(dis_x/4 - (border*2),dis_y - border +(i*dist),Stack[i]);
			draw_text(dis_x/4 - (border*4),dis_y - border,"Panels:");
	}

	draw_set_halign(fa_left);
	draw_text(border,border,"UI Selection" + "    " + string(Selection));
}*/
